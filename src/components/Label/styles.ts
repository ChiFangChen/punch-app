import styled from '@emotion/styled';

const StyledLabel = styled('label')`
  display: inline-block;
  min-width: 140px;
  margin-right: 20px;
  color: #1b76e0;
  font-weight: bold;
`;

export { StyledLabel };
